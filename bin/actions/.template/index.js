require('dotenv').config()
const log = console.log

const new_action = async () => {
  log('--------------------------------------------------------')
  log('[did-cli] new_action')
  log('--------------------------------------------------------')
}

module.exports = new_action
