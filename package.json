{
  "name": "did-cli",
  "version": "0.0.15",
  "description": "",
  "main": "bin/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push && git push --tags"
  },
  "config": {
    "actions": {
      "add_subscription": "Register a new Did subscription. You will be prompted for all necessary information.",
      "import_csv": "Import data to Did from CSV file(s). Specify --path to CSV file to import."
    }
  },
  "keywords": [],
  "license": "MIT",
  "bin": {
    "did": "bin/index.js"
  },
  "dependencies": {
    "boxen": "^4.0.0",
    "chalk": "^2.4.2",
    "csvtojson": "^2.0.10",
    "dotenv": "^8.2.0",
    "inquirer": "^8.0.0",
    "mongodb": "^3.6.4",
    "string-similarity": "^4.0.4",
    "underscore": "^1.12.0",
    "yargs": "^17.0.0-candidate.4"
  },
  "devDependencies": {
    "@commitlint/config-conventional": "^12.0.1",
    "commitlint": "^12.0.1",
    "husky": "^5.1.3"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  }
}
