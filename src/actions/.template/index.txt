require('dotenv').config()
import { log } from '../../utils/log'

export async function action() {
  log('--------------------------------------------------------')
  log('[did-cli] {{name}}')
  log('--------------------------------------------------------')
}
