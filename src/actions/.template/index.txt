require('dotenv').config()
import { log } from '../../utils/log'

export default async () => {
  log('--------------------------------------------------------')
  log('[did-cli] {{name}}')
  log('--------------------------------------------------------')
}
